import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    textWrapper: {
        marginLeft: 5
    },
    buttonWrapper: {
        position: 'fixed',
        bottom: 30,
        right: 30
    }
}));